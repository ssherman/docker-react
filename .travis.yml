language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t ssherman/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true ssherman/docker-react npm run test
